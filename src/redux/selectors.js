export const getFilms = (state) => {
  return state.films;
};

export const getCurrentFilmID = (state) => {
  return state.currentFilmID;
};

export const getSearchStr = (state) => {
  return state.searchStr;
};
